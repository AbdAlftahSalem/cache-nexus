name: cache_nexus_offline_todo
description: Offline-first todo example demonstrating cache_nexus SyncEngine
publish_to: 'none'

environment:
  sdk: ^3.11.5
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cache_nexus:
    path: ../../../
  hive_flutter: ^1.1.0
  connectivity_plus: ^7.1.1
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0

flutter:
  uses-material-design: true
